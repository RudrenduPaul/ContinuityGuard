# Changelog

All notable changes to this project are documented here.

## 0.1.0 -- initial release

First public release. Ships CG01-CG04: clip/frame ingestion, character-
consistency scoring, a physics-plausibility heuristic, and structured
(terminal + JSON) report output. No paid tier, no hosted service -- see
README "What this is not."

### CG02 fixture calibration (real numbers, not illustrative placeholders)

No real short-drama footage was available for this build. Every number
below comes from running the actual built CLI against the synthetic
fixtures committed at `src/score/testdata/clips/` (generated by
`src/score/testdata/generate-fixtures.sh`), via:

```bash
node dist/cli.js scan src/score/testdata/clips --json
```

Real cosine-similarity results from that run:

| Pair | Similarity | Same character claimed? | Flagged? |
|---|---|---|---|
| `mei_shot01.mp4` vs `mei_shot02.mp4` | 0.9975 | yes | no (consistent) |
| `aiko_shot01.mp4` vs `aiko_shot02.mp4` | 0.9906 | yes | no (consistent) |
| `kenji_shot01.mp4` vs `kenji_shot02.mp4` | 0.7709 | yes | **yes** (deliberately inconsistent fixture) |

`CONSISTENCY_SIMILARITY_THRESHOLD` is set to **0.88** -- the midpoint
between the lowest observed consistent-pair similarity (0.9906) and the
observed inconsistent-pair similarity (0.7709), derived from this actual
run, not copied from the execution plan's illustrative 0.62 placeholder.
This threshold is recalibrated whenever the fixture set materially changes
-- see CONTRIBUTING.md "Fixture reproducibility."

**Known caveat, stated honestly:** this is a two-pair, fully synthetic
calibration (solid-color fixtures, not real faces), not a statistically
robust threshold derived from a large labeled dataset. It is a real,
reproducible starting point for v0.1, not a claim that 0.88 is the
"correct" threshold for real photorealistic or stylized footage. Expect
this number to move as real-world usage reports come in (see README
"Known limitations").

### CG03 fixture calibration (real numbers, not illustrative placeholders)

Real frame-to-frame diff results from the same run, over
`action-discontinuity.mp4` (a clip with one deliberate abrupt jump) and
`calm-baseline.mp4` (smooth motion throughout, no jump):

| Clip | Frame-to-frame diffs (normalized) | Median baseline | Flagged transitions |
|---|---|---|---|
| `action-discontinuity.mp4` | 0.0185, 0.0245, 0.0184, 0.0186, **0.4952**, **0.4993**, 0.0569, 0.0631, 0.0691, 0.0714 | 0.060 | frame 4-5 at 8.25x, frame 5-6 at 8.32x |
| `calm-baseline.mp4` | 0.0184, 0.0244, 0.0184, 0.0183, 0.0254, 0.0183, 0.0223, 0.0183, 0.0232 | 0.0223 | none (max ratio ~1.14x) |

`DISCONTINUITY_MULTIPLIER` is set to **3** -- comfortably above
`calm-baseline.mp4`'s real observed max ratio (~1.14x, no false positive)
and well below the deliberate discontinuity's real observed ratios
(8.25x-8.32x), derived from this actual run, not the execution plan's
illustrative 4.1x placeholder.

### Zero-network guarantee, verified empirically

`npm run verify:zero-network` (`scripts/verify-zero-network.mjs`)
monkey-patches every network entry point Node exposes (`fetch`,
`net.Socket.connect`, `net.createConnection`, `tls.connect`, `dns.lookup`,
`dns.resolve`, `http(s).request`, `http(s).get`) to throw, then runs a real
`scan` against the committed fixtures. The scan completes successfully
with zero patched functions triggered -- this is runtime proof, not just a
source-code grep, that no code path in `scan` makes an outbound network
call. This check runs in CI on every push.

### Runtime note: onnxruntime-node vs. a WASM fallback

The execution plan flagged a real risk: an earlier, unrelated prototype in
this same portfolio hit a native-binding crash using
`@tensorflow/tfjs-node` on Node 24 / macOS ARM, and had to fall back to a
pure-JS/WASM backend. `onnxruntime-node` was smoke-tested directly on that
same machine/Node/architecture combination before writing any scoring code
(loading a real `InferenceSession` and running real inference against a
small ONNX model succeeded without error). It did not reproduce the
tfjs-node crash, so v0.1 ships `onnxruntime-node` as originally planned --
no WASM runtime fallback was necessary. See
`src/score/models/NOTICE.md` for the one substitution that *was* made
(the bundled model, not the runtime).
